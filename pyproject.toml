[project]
name = "ai-campus-admin-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "sqlalchemy>=2.0.43",
]

[tool.uv.workspace]
members = [
    "campus-admin-agent",
]
